local DSSM_Model = {}
DSSM_Model.__index = DSSM_Model

require 'nngraph'
require 'nn'


-- define the dssm model for each sublayer. 
function DSSM_Model.init(feature_size, layer_dim, activation, sigma, arch, wind)


end

return DSSM_Model